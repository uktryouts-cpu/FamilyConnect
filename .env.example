# ============================================================================
# 🔒 FamilyConnect Environment Variables
# ============================================================================
# This file shows EXAMPLE values for LOCAL DEVELOPMENT ONLY
# 
# IMPORTANT SECURITY NOTES:
# 1. NEVER commit actual secrets to git
# 2. Add .env.local and .env.production to .gitignore (already done)
# 3. For production, use secure vault (AWS Secrets Manager, HashiCorp Vault)
# 4. API keys should NEVER be exposed to the browser
# ============================================================================

# ============================================================================
# REQUIRED: API Configuration
# ============================================================================

# Gemini (LLM) API key - USED SERVER-SIDE ONLY
# Get from: https://ai.google.dev/
# ⚠️  NEVER expose to browser - this causes quota consumption by attackers
GEMINI_API_KEY=AIza...

# ============================================================================
# REQUIRED: Security Configuration
# ============================================================================

# JWT Secret for signing authentication tokens
# MUST be at least 32 random characters
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# For local development, you can use a placeholder
# For production, use a strong random secret
JWT_SECRET=your-32-character-random-secret-change-in-production

# Token expiration time in seconds
# Default: 3600 (1 hour)
# Must be between 300 (5 min) and 86400 (24 hours)
TOKEN_EXPIRY=3600

# ============================================================================
# OPTIONAL: CORS Configuration
# ============================================================================

# Whitelisted origins for CORS
# Comma-separated list of allowed domains
# Default: http://localhost:3000,http://localhost:5173,http://localhost:5174
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost:5174

# ============================================================================
# OPTIONAL: Rate Limiting
# ============================================================================

# Rate limit window in milliseconds
# Default: 900000 (15 minutes)
RATE_LIMIT_WINDOW_MS=900000

# Maximum requests per window per IP
# Default: 100 requests per 15 minutes
RATE_LIMIT_MAX_REQUESTS=100

# ============================================================================
# OPTIONAL: Data Protection
# ============================================================================

# Salt for hashing PII (Personally Identifiable Information)
# MUST be at least 16 random characters
# Generate with: node -e "console.log(require('crypto').randomBytes(16).toString('hex'))"
# Used for duplicate detection without exposing names/dates
PII_HASH_SALT=your-16-character-random-salt

# ============================================================================
# OPTIONAL: Environment & Logging
# ============================================================================

# Application environment
# Options: development, staging, production
# Default: development
NODE_ENV=development

# Server port
# Default: 5174
PORT=5174

# Logging level
# Options: debug, info, warn, error
# Default: info
LOG_LEVEL=info

# ============================================================================
# OPTIONAL: Database (for future use)
# ============================================================================

# PostgreSQL connection string for user data and billing
# Format: postgresql://username:password@hostname:5432/database
# Required for production deployment
# DATABASE_URL=postgresql://user:password@localhost:5432/familyconnect

# ============================================================================
# OPTIONAL: Stripe Integration (for future billing)
# ============================================================================

# Stripe secret key for payment processing
# Get from: https://dashboard.stripe.com/
# Only needed if ENABLE_BILLING=true
# STRIPE_SECRET_KEY=sk_live_...

# Stripe webhook endpoint secret
# Get from: https://dashboard.stripe.com/webhooks
# STRIPE_WEBHOOK_SECRET=whsec_...

# ============================================================================
# OPTIONAL: Email Service (for future notifications)
# ============================================================================

# SendGrid API key for sending transactional emails
# Get from: https://app.sendgrid.com/settings/api_keys
# SENDGRID_API_KEY=SG...

# Mailgun API key (alternative to SendGrid)
# MAILGUN_API_KEY=...

# ============================================================================
# OPTIONAL: Feature Flags
# ============================================================================

# Enable subscription billing system
# Options: true, false
# Default: false
ENABLE_BILLING=false

# Enable JWT authentication
# Options: true, false
# Default: false
ENABLE_AUTHENTICATION=false

# ============================================================================
# ⚠️  SECURITY REMINDERS
# ============================================================================
# - Use .env.local for local development (added to .gitignore)
# - Use .env.production for production secrets (NOT in git)
# - Rotate API keys quarterly
# - NEVER share secrets via email, Slack, or chat
# - Use a password manager to store complex secrets
# - Enable 2FA on all API key management platforms
# ============================================================================
